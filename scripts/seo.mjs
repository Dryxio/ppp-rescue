import {readFileSync,writeFileSync} from 'node:fs';
import {createHash} from 'node:crypto';
const schema=JSON.stringify({'@context':'https://schema.org','@type':'WebApplication',name:'PPP Rescue',url:'https://recoverppp.com/',description:'Free local text and image recovery for supported ZIP/XML Serif PagePlus .ppp files.',applicationCategory:'UtilitiesApplication',operatingSystem:'Any OS with a modern web browser',browserRequirements:'Requires JavaScript',isAccessibleForFree:true,offers:{'@type':'Offer',price:'0',priceCurrency:'USD'},softwareVersion:'0.1.0',license:'https://github.com/Dryxio/ppp-rescue/blob/main/LICENSE'});
let html=readFileSync('index.html','utf8').replace(/<script type="application\/ld\+json">.*?<\/script>/s,'');html=html.replace('</head>',`<script type="application/ld+json">${schema}</script></head>`);writeFileSync('index.html',html);
const config=JSON.parse(readFileSync('vercel.json'));const h=config.headers[0].headers.find(h=>h.key==='Content-Security-Policy');h.value=h.value.replace(/'sha256-[^']*'/,`'sha256-${createHash('sha256').update(schema).digest('base64')}'`);writeFileSync('vercel.json',JSON.stringify(config,null,2)+'\n');
